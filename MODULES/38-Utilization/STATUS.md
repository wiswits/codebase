# 38 · Utilization

**Domain:** Staff & HR
**Status:** HOLD — blocked upstream + low priority

**What it is:** Workforce utilization analytics. Waits for upstream assembly AND a value case.

## Features
- (spec only) capacity, bench strength, dept utilization

See `CODE_MAP.md` for where the LIVE code is, `PRD/PRD.md` for the spec.

---

## Intake — the intern build for this module

| Build (in `intake/`) | Files | State | What is in the way |
|---|---|---|---|
| `intake/Utilization-Management/` | 237 | ⏳ PENDING | `incoming/` plus `not integrated/`, never assembled |

Its own verdict, spec and source are inside that folder (`STATUS.md` · `PRD/` · `code/`). Port, never cut-paste — see `MODULES/INTAKE_ROADMAP.md`.
