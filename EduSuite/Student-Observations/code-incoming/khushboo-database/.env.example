# ============================================================
# EduSuite - Student Observations
# Example Local Database Configuration
# ============================================================

DB_HOST=localhost
DB_PORT=3307
DB_NAME=edusuite_student_observations
DB_USER=root
DB_PASSWORD=your_local_password

# Never commit a real .env file.
# Never commit production credentials.
#
# During final EduSuite integration, the module must use the
# parent platform's shared database infrastructure.