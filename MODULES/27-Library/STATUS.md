# 27 · Library

**Domain:** Student Life
**Status:** LIVE

**What it is:** Physical library circulation. Intern build = diff-then-port-gaps-only (P2 dedup).

## Features
- Books, members, issues, categories
- Issue/return with race-condition-safe locking

See `CODE_MAP.md` for where the LIVE code is, `PRD/PRD.md` for the spec.

---

## Intake — the intern/EduSuite build for this module

| Build (in `intake/`) | Files | State | What is in the way |
|---|---|---|---|
| `intake/Library-Management/` | 53 | ⏳ PENDING | Feature-by-feature diff against `erp/library` |

Its own verdict, spec and source are inside that folder (`STATUS.md` · `PRD/` · `code/`). Port, never cut-paste — see `MODULES/INTAKE_ROADMAP.md`.
