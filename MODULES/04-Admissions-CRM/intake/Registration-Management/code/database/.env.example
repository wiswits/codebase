# ============================================================
# WisWits Registration Management
# Example Database Environment Configuration
#
# DO NOT place real production secrets in this file.
# Copy these values into the backend .env and replace them
# locally as required.
# ============================================================

DB_HOST=localhost
DB_PORT=3307

DB_NAME=wiswits_registration

DB_USER=root
DB_PASSWORD=your_local_mariadb_password

DB_CONNECTION_LIMIT=10