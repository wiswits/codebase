# 14 · Question Bank

**Domain:** Academics
**Status:** LIVE

**What it is:** The question store powering quizzes, worksheets, and paper generation. Cross-tenant reads ONLY via libraryScope.js (the sanctioned scope).

## Features
- Subjects/topics/chapters taxonomy
- Question CRUD + import (RESULT: CLEAN protocol)
- Cross-org platform library via sanctioned libraryScope

See `CODE_MAP.md` for where the LIVE code is, `PRD/PRD.md` for the spec.
