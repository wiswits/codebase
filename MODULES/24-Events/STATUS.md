# 24 · Events

**Domain:** Student Life
**Status:** LIVE

**What it is:** Full event lifecycle — the first EduSuite intake, now richer than its source.

## Features
- RSVP + capacity locking
- Resource booking
- Gallery album link
- Lifecycle + printable RSVP

See `CODE_MAP.md` for where the LIVE code is, `PRD/PRD.md` for the spec.

---

## Intake — the intern/EduSuite build for this module

| Build (in `intake/`) | Files | State | What is in the way |
|---|---|---|---|
| `intake/Event-Management/` | 104 | ✅ **MERGED** | LIVE to every school. Ported to `apps/backend/src/modules/events/` — 29 files · 1,893 lines |

Its own verdict, spec and source are inside that folder (`STATUS.md` · `PRD/` · `code/`). Port, never cut-paste — see `MODULES/INTAKE_ROADMAP.md`.
