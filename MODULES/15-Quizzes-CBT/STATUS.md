# 15 · Quizzes CBT

**Domain:** Academics
**Status:** LIVE

**What it is:** Computer-based testing. Coaching & Test Series intern build = future feature mine (batches, OMR, doubts, error-book) — additions here, not a new module.

## Features
- Quiz create/attempt/analytics
- Parent view
- Separate quiz-portal app for standalone use

See `CODE_MAP.md` for where the LIVE code is, `PRD/PRD.md` for the spec.

---

## Intake — the intern/EduSuite build for this module

| Build (in `intake/`) | Files | State | What is in the way |
|---|---|---|---|
| `intake/Coaching-and-Test-Series/` | 118 | ⏳ PENDING | Mine batches / OMR / doubts into `lms` and `qbank` — do not duplicate |

Its own verdict, spec and source are inside that folder (`STATUS.md` · `PRD/` · `code/`). Port, never cut-paste — see `MODULES/INTAKE_ROADMAP.md`.
