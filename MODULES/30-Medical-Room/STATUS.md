# 30 · Medical Room

**Domain:** Student Life
**Status:** HOLD — §16-gated non-goal, AK's explicit call required

**What it is:** Campus healthcare records. Explicitly gated until Phase 1 is stable AND AK green-lights.

## Features
- (spec only) medical records, treatments, medicine inventory

See `CODE_MAP.md` for where the LIVE code is, `PRD/PRD.md` for the spec.

---

## Intake — the intern build for this module

| Build (in `intake/`) | Files | State | What is in the way |
|---|---|---|---|
| `intake/Medical-Room/` | 3 | ⏳ PENDING | Written CONTRACT only — no code anywhere. Build from the contract |

Its own verdict, spec and source are inside that folder (`STATUS.md` · `PRD/` · `code/`). Port, never cut-paste — see `MODULES/INTAKE_ROADMAP.md`.
