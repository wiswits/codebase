# Borrowed pages removed (de-dup 2026-08-12)

These frontend pages are OWNED by other modules; their duplicate copies were
removed from here. Browse them in the owner module:

- `(dashboard)/academics/curriculum/` + `.../curriculum/setup/` → **18-Curriculum-CIE**
- `(dashboard)/academics/timetable/` → **09-Timetable**

08-Classes-and-Sections keeps only its own backend `erp/classes.routes.js`.
