# Shared code — not duplicated here

This module's code is **shared** and now lives once in the SHARED tier
(no per-module copy, so it can never drift out of sync):

- `_shared/domains/hr/` (backend `hr/` + web `(dashboard)/hr/`)

> De-dup pass 2026-08-12: identical copies that used to sit in each module
> folder were collapsed to a single canonical copy. Browse/edit it there.
