# 23 · Wellbeing and Happiness

**Domain:** Student Life
**Status:** SOON-GATED — cycle 1 ported and on prod (the `code/` snapshot predates it; live paths are in `CODE_MAP.md`). Cycle 2 = counsellor desk

**What it is:** The Care Layer. First module in the post-launch conveyor. Its 21 guardrail promises are part of the spec, not optional.

## Features
- Student journal with guardrails (care not surveillance)
- Counsellor + teacher screens
- Crisis protocol
- Append-only audit triggers

See `CODE_MAP.md` for where the LIVE code is, `PRD/PRD.md` for the spec.

---

## Intake — the intern build for this module

| Build (in `intake/`) | Files | State | What is in the way |
|---|---|---|---|
| `intake/Wellbeing-and-Happiness/` | 97 | ✅ **MERGED** | Cycle 1 on prod, `soon`-gated — 41+4 files · 2,942 lines. Cycle 2 = counsellor desk |

Its own verdict, spec and source are inside that folder (`STATUS.md` · `PRD/` · `code/`). Port, never cut-paste — see `MODULES/INTAKE_ROADMAP.md`.
