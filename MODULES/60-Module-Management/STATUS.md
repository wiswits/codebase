# 60 · Module Management

**Domain:** Platform
**Status:** BUILT, DARK — Features page unbuilt (BE mounted)

**What it is:** The control plane deciding what each school sees. Superadmin side is live; the tenant-facing toggle UI is the one genuinely unbuilt piece.

## Features
- Per-org module on/off (nav_flags)
- Plan gating (ON for new orgs, off for legacy)
- Org Control Room (superadmin) live
- Tenant-side Settings > Features page = the missing UI

See `CODE_MAP.md` for where the LIVE code is, `PRD/PRD.md` for the spec.
