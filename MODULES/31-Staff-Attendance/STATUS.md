# 31 · Staff Attendance

**Domain:** Staff & HR
**Status:** LIVE

**What it is:** Employee attendance with geofencing. the Biometric intern contract = future device-sync layer on this, not a new module.

## Features
- Day view, check-in/out, late-by minutes
- Audit-logged override
- Geofence policy editor
- Biometric device config in Settings

See `CODE_MAP.md` for where the LIVE code is, `PRD/PRD.md` for the spec.

---

## Intake — the intern build for this module

| Build (in `intake/`) | Files | State | What is in the way |
|---|---|---|---|
| `intake/Biometric-Attendance/` | 3 | ⏳ PENDING | Written CONTRACT only — no code anywhere. Build from the contract |

Its own verdict, spec and source are inside that folder (`STATUS.md` · `PRD/` · `code/`). Port, never cut-paste — see `MODULES/INTAKE_ROADMAP.md`.
