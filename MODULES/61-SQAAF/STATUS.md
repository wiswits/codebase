# 61 · SQAAF

**Domain:** Platform
**Status:** GAP — no code anywhere, written contract only

**What it is:** School Quality Assurance and Assessment Framework — the self-assessment
and quality-audit layer a school runs against itself. The one intern module with **no
native counterpart at all**, which is why it holds a slot of its own rather than sitting
inside another module.

## Features (per the contract — none built)
- Quality domains + indicators
- School self-assessment cycle
- Evidence upload against each indicator
- Scoring + improvement plan

See `CODE_MAP.md` for where the LIVE code is, `intake/SQAAF/PRD/` for the spec.

---

## Intake — the intern build for this module

| Build (in `intake/`) | Files | State | What is in the way |
|---|---|---|---|
| `intake/SQAAF/` | 3 | ⏳ PENDING | Written CONTRACT only — no code anywhere. Build from the contract |

Its own verdict, spec and source are inside that folder (`STATUS.md` · `PRD/` · `code/`). Port, never cut-paste — see `MODULES/INTAKE_ROADMAP.md`.
