# 54 · Custom Fields

**Domain:** Platform
**Status:** LIVE

**What it is:** Schools extend records without code — configuration over customization (§3).

## Features
- No-code field engine on students/staff
- DynamicForm renderer

See `CODE_MAP.md` for where the LIVE code is, `PRD/PRD.md` for the spec.
