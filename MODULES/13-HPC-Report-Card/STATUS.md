# 13 · HPC Report Card

**Domain:** Academics
**Status:** GAP — build ready, blocked on AK's call: who supplies the competency list

**What it is:** NEP-mandated holistic progress format. Ports as a template/extension of Report-Cards (#12), keeping one report-card system.

## Features
- NEP Holistic Progress Card
- Competency workspace by cycle
- Card preview + summary

See `CODE_MAP.md` for where the LIVE code is, `PRD/PRD.md` for the spec.

---

## Intake — the intern/EduSuite build for this module

| Build (in `intake/`) | Files | State | What is in the way |
|---|---|---|---|
| `intake/HPC-Report-Card/` | 106 | ⏳ PENDING | One cycle of engineering, but AK must first decide who supplies the competency list |

Its own verdict, spec and source are inside that folder (`STATUS.md` · `PRD/` · `code/`). Port, never cut-paste — see `MODULES/INTAKE_ROADMAP.md`.
