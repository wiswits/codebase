# 35 · Performance PMS

**Domain:** Staff & HR
**Status:** THIN — scoped extension pending

**What it is:** Appraisal-season workflows for schools — goal setting and review cycles — WITHOUT becoming corporate HR software (§16 risk flagged).

## Features
- Basic scorecard exists (see #33)
- the HR-PMS intern build = goals/review-cycles source, must be scoped school-sized

See `CODE_MAP.md` for where the LIVE code is, `PRD/PRD.md` for the spec.

---

## Intake — the intern build for this module

| Build (in `intake/`) | Files | State | What is in the way |
|---|---|---|---|
| `intake/HR-PMS/` | 99 | ⏳ PENDING | Scope to school-sized before porting (risk of reading as generic corporate HR, §16) |

Its own verdict, spec and source are inside that folder (`STATUS.md` · `PRD/` · `code/`). Port, never cut-paste — see `MODULES/INTAKE_ROADMAP.md`.
