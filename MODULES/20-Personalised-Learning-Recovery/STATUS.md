# 20 · Personalised Learning Recovery

**Domain:** Academics
**Status:** LIVE (thin) — upgrade path ready

**What it is:** Turns every test into a diagnosis. Anil's build ports as RECOVERY V2 inside this module — never a parallel module.

## Features
- Weak-area detection
- Recovery insights for teachers
- UPGRADE SOURCE: Anil's engine (SM2, root-cause, recovery cycles, 163 tests)

See `CODE_MAP.md` for where the LIVE code is, `PRD/PRD.md` for the spec.

---

## Intake — the intern/EduSuite build for this module

| Build (in `intake/`) | Files | State | What is in the way |
|---|---|---|---|
| `intake/Personalised-Learning/` | 123 | ✅ **MERGED** | Cycle 1 built, awaiting deploy — inside `recovery/engines/` + `insights/`, 20 files · 3,302 lines |

Its own verdict, spec and source are inside that folder (`STATUS.md` · `PRD/` · `code/`). Port, never cut-paste — see `MODULES/INTAKE_ROADMAP.md`.
