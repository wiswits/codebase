// Thin route wrapper — the real page lives in the co-located module folder
// (modules/academics/pages/LearnPage). Framework needs a file here; the code
// is managed under modules/academics.
export { default } from "@/modules/academics/pages/LearnPage";
